{
  "name": "vault",
  "private": true,
  "version": "1.0.0",
  "description": "Vault — personal knowledge hub. Notes, YouTube, PDFs/books and documents in one place, with date stamps, project tags, a graph view and a dashboard.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "gh-pages": "^6.2.0",
    "vite": "^5.4.11",
    "vite-plugin-singlefile": "^2.3.3"
  }
}
